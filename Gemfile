source 'https://rubygems.org'
group :jekyll_plugins do
# github-pages forces an old version of jekyll-scholar
# which in turn results in build errors.
# We don't really need github-pages here, so workaround until proper solution
#    gem 'github-pages'
    gem 'kramdown-parser-gfm'
    gem 'jekyll'
    gem 'jekyll-email-protect'
    gem 'jekyll-paginate'
    gem 'jekyll-scholar'
    gem 'jemoji'
    gem 'unicode_utils'
end
